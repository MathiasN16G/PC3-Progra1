@model List<PC3_Progra1.Models.Feedback>

<h2 class="text-center mb-4">Historial de Reacciones</h2>

<table class="table table-hover table-bordered shadow-sm">
    <thead class="table-primary text-center">
        <tr>
            <th>#</th>
            <th>Post ID</th>
            <th>Reacción</th>
            <th>Fecha</th>
        </tr>
    </thead>
    <tbody class="text-center">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.PostId</td>
                <td>
                    @if (item.Sentimiento == "like")
                    {
                        <span class="text-success fw-bold">👍 Me gustó</span>
                    }
                    else if (item.Sentimiento == "dislike")
                    {
                        <span class="text-danger fw-bold">👎 No me gustó</span>
                    }
                </td>
                <td>@item.Fecha.ToLocalTime().ToString("g")</td>
            </tr>
        }
    </tbody>
</table>
